# 360 Degree Rotation Test Scenario
# Tests scan matching during pure rotation

name: "360 Rotation Test"
description: "Rotate in place exactly 360 degrees CCW"

map: "medium_room"

seed:
  scans: 20
  rotation: true

commands:
  # Full 360° CCW rotation in place
  - type: rotate
    degrees: 360

svg_output:
  scale: 50.0
  marker_interval_s: 1.0
  trajectory_width: 2.0
